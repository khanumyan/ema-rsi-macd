# 📊 Полная логика работы стратегии EMA+RSI+MACD

## 🎯 Общий обзор

Стратегия **EMA+RSI+MACD** - это мультииндикаторная торговая стратегия, которая использует:
- **EMA** (Exponential Moving Average) - для определения тренда
- **RSI** (Relative Strength Index) - для определения перекупленности/перепроданности
- **MACD** (Moving Average Convergence Divergence) - для определения импульса
- **ATR** (Average True Range) - для расчета уровней Stop Loss и Take Profit

Стратегия работает по **балльной системе**, где каждый индикатор и их комбинации дают определенное количество баллов. Баллы конвертируются в вероятности, на основе которых определяется финальный сигнал (BUY/SELL/HOLD) и его сила (STRONG/MEDIUM/WEAK).

---

## ⚙️ Параметры стратегии (по умолчанию)

| Параметр | Значение | Описание |
|----------|----------|----------|
| `ema_fast` | 20 | Период быстрой EMA |
| `ema_slow` | 50 | Период медленной EMA |
| `rsi_period` | 14 | Период RSI |
| `macd_fast` | 12 | Быстрый период MACD |
| `macd_slow` | 26 | Медленный период MACD |
| `macd_signal` | 9 | Период сигнальной линии MACD |
| `atr_period` | 14 | Период ATR |
| `interval` | 15m | Таймфрейм свечей |
| `limit` | 200 | Количество свечей для анализа |
| `stop_loss_multiplier` | 2.3 | Множитель для Stop Loss |
| `take_profit_multiplier` | 2.0 | Множитель для Take Profit |

---

## 📈 Шаг 1: Получение и расчет данных

### 1.1 Получение данных с биржи
- Источник: Binance Futures API (`https://fapi.binance.com/fapi/v1/klines`)
- Извлекаются: High, Low, Close цены
- Используется последняя закрытая свеча как текущая цена

### 1.2 Расчет индикаторов

#### EMA (Exponential Moving Average)
```
Множитель = 2 / (период + 1)
EMA = (Цена × Множитель) + (Предыдущая EMA × (1 - Множитель))
```
- **EMA(20)** - быстрая скользящая средняя
- **EMA(50)** - медленная скользящая средняя

#### RSI (Relative Strength Index)
```
RS = Средний прирост / Средний убыток
RSI = 100 - (100 / (1 + RS))
```
- Период: 14
- Диапазон: 0-100
- Метод сглаживания: Wilder's smoothing

#### MACD (Moving Average Convergence Divergence)
```
MACD Line = EMA(12) - EMA(26)
Signal Line = EMA(9) от MACD Line
Histogram = MACD Line - Signal Line
```

#### ATR (Average True Range)
```
True Range = max(
    High - Low,
    |High - Previous Close|,
    |Low - Previous Close|
)
ATR = Среднее значение последних 14 True Range
```

---

## 🎲 Шаг 2: Балльная система

Стратегия использует **две независимые системы баллов**:
- `long_score` - баллы за условия роста (BUY)
- `short_score` - баллы за условия падения (SELL)

### 2.1 Условия для BUY (long_score)

#### Условие 1: Основное условие (+40 баллов)
**Требования:**
```
price > EMA20  AND  EMA20 > EMA50  AND  MACD Line > 0  AND  Histogram > 0  AND  RSI < 70
```

**Логика:**
- Цена выше быстрой EMA → бычий тренд
- EMA20 выше EMA50 → подтверждение бычьего тренда
- MACD Line > 0 → бычий импульс
- Histogram > 0 → усиление бычьего импульса
- RSI < 70 → исключает перекупленность

**Дополнительные баллы за импульс:**
- Если `RSI 40-60` И `|Histogram| > 0.5` → **+30 баллов** (сильный импульс)
- Если `RSI > 30` И `|Histogram| > 0.2` → **+15 баллов** (средний импульс)

#### Условие 2: Тренд без MACD (+20 баллов)
**Требования:**
```
price > EMA20  AND  EMA20 > EMA50
```
- Работает, если основное условие не выполнено
- Только тренд, без подтверждения MACD

#### Условие 3: Перепроданность RSI (+20 баллов)
**Требования:**
```
RSI ≤ 30
```
- Независимо от других условий
- Зона перепроданности → возможен отскок вверх

#### Условие 4: Бычье пересечение MACD (+10 баллов)
**Требования:**
```
Histogram > 0  AND  MACD Line > Signal Line
```
- Независимо от других условий
- MACD пересекает Signal Line вверх → бычий сигнал

**Максимальные баллы для BUY: 100** (40 + 30 + 20 + 10)

---

### 2.2 Условия для SELL (short_score)

#### Условие 1: Основное условие (+40 баллов)
**Требования:**
```
price < EMA20  AND  EMA20 < EMA50  AND  MACD Line < 0  AND  Histogram < 0  AND  RSI > 30
```

**Логика:**
- Цена ниже быстрой EMA → медвежий тренд
- EMA20 ниже EMA50 → подтверждение медвежьего тренда
- MACD Line < 0 → медвежий импульс
- Histogram < 0 → усиление медвежьего импульса
- RSI > 30 → исключает перепроданность

**Дополнительные баллы за импульс:**
- Если `RSI 40-60` И `|Histogram| > 0.5` → **+30 баллов** (сильный импульс)
- Если `RSI < 70` И `|Histogram| > 0.2` → **+15 баллов** (средний импульс)

#### Условие 2: Тренд без MACD (+20 баллов)
**Требования:**
```
price < EMA20  AND  EMA20 < EMA50
```
- Работает, если основное условие не выполнено
- Только тренд, без подтверждения MACD

#### Условие 3: Перекупленность RSI (+20 баллов)
**Требования:**
```
RSI ≥ 70
```
- Независимо от других условий
- Зона перекупленности → возможен откат вниз

#### Условие 4: Медвежье пересечение MACD (+10 баллов)
**Требования:**
```
Histogram < 0  AND  MACD Line < Signal Line
```
- Независимо от других условий
- MACD пересекает Signal Line вниз → медвежий сигнал

**Максимальные баллы для SELL: 100** (40 + 30 + 20 + 10)

---

## 🔄 Шаг 3: Конвертация баллов в вероятности

Баллы конвертируются в проценты вероятности:

```php
total_score = long_score + short_score

if (total_score > 0) {
    long_probability = (long_score / total_score) × 100
    short_probability = (short_score / total_score) × 100
} else {
    long_probability = 50
    short_probability = 50
}
```

**Важно:** Вероятности всегда в сумме дают 100%.

**Примеры:**
- `long_score = 70, short_score = 30` → `long_prob = 70%, short_prob = 30%`
- `long_score = 50, short_score = 50` → `long_prob = 50%, short_prob = 50%`
- `long_score = 0, short_score = 0` → `long_prob = 50%, short_prob = 50%`

---

## 🎯 Шаг 4: Определение сигнала

### 4.1 Тип сигнала (BUY/SELL/HOLD)

```php
if (long_probability > short_probability AND long_probability > 50) {
    signal = 'BUY'
} elseif (short_probability > long_probability AND short_probability > 50) {
    signal = 'SELL'
} else {
    signal = 'HOLD'
}
```

**Логика:**
- **BUY**: Вероятность роста > вероятности падения И вероятность роста > 50%
- **SELL**: Вероятность падения > вероятности роста И вероятность падения > 50%
- **HOLD**: Во всех остальных случаях (равенство или обе вероятности ≤ 50%)

### 4.2 Сила сигнала (STRONG/MEDIUM/WEAK)

```php
probDifference = |long_probability - short_probability|

if (probDifference > 20) {
    strength = 'STRONG'
} elseif (probDifference > 10) {
    strength = 'MEDIUM'
} else {
    strength = 'WEAK'
}
```

**Логика:**
- **STRONG**: Разница вероятностей > 20% (например, 80% vs 20%)
- **MEDIUM**: Разница вероятностей > 10% (например, 65% vs 35%)
- **WEAK**: Разница вероятностей ≤ 10% (например, 55% vs 45%)

**Важно:** Сила сигнала зависит от **разницы** между вероятностями, а не от абсолютных значений баллов.

---

## 🛡️ Шаг 5: Расчет уровней Stop Loss и Take Profit

Используется ATR (Average True Range) для динамического расчета уровней:

### Для BUY сигнала:
```
Stop Loss = price - (ATR × 2.3)
Take Profit = price + (ATR × 2.0)
```

### Для SELL сигнала:
```
Stop Loss = price + (ATR × 2.3)
Take Profit = price - (ATR × 2.0)
```

**Логика:**
- Stop Loss дальше от цены (×2.3), чтобы дать больше пространства
- Take Profit ближе к цене (×2.0), чтобы быстрее фиксировать прибыль
- ATR адаптируется к волатильности рынка

---

## 🔍 Шаг 6: Проверка рыночного контекста (опционально)

Перед отправкой сигнала проверяется волатильность BTC:

### Правило 1: Высокая волатильность BTC
```
Если BTC волатильность > 3% за 15m → блокировать ВСЕ сигналы
```

### Правило 2: Падение BTC
```
Если BTC падает > 1% за 15m → блокировать SELL сигналы для альткоинов
```

**Логика:**
- Защита от ложных сигналов в нестабильном рынке
- Альткоины часто следуют за BTC, поэтому SELL на альтах при падении BTC может быть ложным

---

## 📊 Примеры работы стратегии

### Пример 1: Сильный BUY сигнал

**Данные:**
```
Price = 50000
EMA20 = 49800
EMA50 = 49500
RSI = 45
MACD Line = 50
MACD Signal = 30
MACD Histogram = 20
ATR = 500
```

**Расчет баллов:**
1. Основное условие: ✓ (price > EMA20, EMA20 > EMA50, MACD > 0, Hist > 0, RSI < 70) → +40
2. Сильный импульс: ✓ (RSI 40-60, |Hist| = 20 > 0.5) → +30
3. Перепроданность: ✗ (RSI = 45 > 30) → 0
4. Бычье пересечение: ✓ (Hist > 0, MACD > Signal) → +10

**Итого:**
- `long_score = 80`
- `short_score = 0`
- `long_probability = 100%`
- `short_probability = 0%`
- **Сигнал: BUY, STRONG** (разница = 100%)

**Уровни:**
- Stop Loss = 50000 - (500 × 2.3) = 48850
- Take Profit = 50000 + (500 × 2.0) = 51000

---

### Пример 2: Слабый BUY сигнал

**Данные:**
```
Price = 50000
EMA20 = 49800
EMA50 = 49500
RSI = 35
MACD Line = 5
MACD Signal = 3
MACD Histogram = 2
ATR = 500
```

**Расчет баллов:**
1. Основное условие: ✓ → +40
2. Сильный импульс: ✗ (|Hist| = 2 > 0.5, но RSI = 35 не в зоне 40-60)
3. Средний импульс: ✓ (RSI > 30, |Hist| = 2 > 0.2) → +15
4. Перепроданность: ✗ (RSI = 35 > 30) → 0
5. Бычье пересечение: ✓ → +10

**Итого:**
- `long_score = 65`
- `short_score = 0`
- `long_probability = 100%`
- `short_probability = 0%`
- **Сигнал: BUY, STRONG**

---

### Пример 3: Противоречивый сигнал (HOLD)

**Данные:**
```
Price = 50000
EMA20 = 49800
EMA50 = 49500
RSI = 65
MACD Line = -10
MACD Signal = -5
MACD Histogram = -5
ATR = 500
```

**Расчет баллов BUY:**
1. Основное условие: ✗ (MACD < 0)
2. Тренд без MACD: ✓ (price > EMA20, EMA20 > EMA50) → +20
3. Перепроданность: ✗ (RSI = 65 > 30) → 0
4. Бычье пересечение: ✗ (Hist < 0) → 0

**Расчет баллов SELL:**
1. Основное условие: ✗ (price > EMA20)
2. Тренд без MACD: ✗ (price > EMA20)
3. Перекупленность: ✗ (RSI = 65 < 70) → 0
4. Медвежье пересечение: ✓ (Hist < 0, MACD < Signal) → +10

**Итого:**
- `long_score = 20`
- `short_score = 10`
- `long_probability = 67%`
- `short_probability = 33%`
- **Сигнал: BUY, WEAK** (разница = 34%, но long_prob > 50%)

---

### Пример 4: Сильный SELL сигнал

**Данные:**
```
Price = 50000
EMA20 = 50200
EMA50 = 50500
RSI = 75
MACD Line = -50
MACD Signal = -30
MACD Histogram = -20
ATR = 500
```

**Расчет баллов:**
1. Основное условие: ✓ (price < EMA20, EMA20 < EMA50, MACD < 0, Hist < 0, RSI > 30) → +40
2. Сильный импульс: ✓ (RSI 40-60? Нет, но |Hist| = 20 > 0.5) → проверка не пройдена
3. Средний импульс: ✓ (RSI < 70? Нет, RSI = 75) → проверка не пройдена
4. Перекупленность: ✓ (RSI = 75 ≥ 70) → +20
5. Медвежье пересечение: ✓ (Hist < 0, MACD < Signal) → +10

**Итого:**
- `long_score = 0`
- `short_score = 70`
- `long_probability = 0%`
- `short_probability = 100%`
- **Сигнал: SELL, STRONG** (разница = 100%)

**Уровни:**
- Stop Loss = 50000 + (500 × 2.3) = 51150
- Take Profit = 50000 - (500 × 2.0) = 49000

---

## 🎓 Ключевые принципы стратегии

### 1. Мультииндикаторный подход
- Стратегия не полагается на один индикатор
- Все индикаторы должны подтверждать друг друга для сильного сигнала

### 2. Балльная система
- Каждое условие имеет свой вес
- Баллы суммируются независимо для BUY и SELL
- Важна не абсолютная величина баллов, а их соотношение

### 3. Вероятностный подход
- Баллы конвертируются в вероятности
- Сигнал генерируется только при вероятности > 50%
- Сила сигнала зависит от разницы вероятностей

### 4. Защита от ложных сигналов
- RSI фильтрует экстремальные зоны
- MACD подтверждает импульс
- EMA определяет тренд
- Проверка рыночного контекста (BTC волатильность)

### 5. Динамические уровни
- Stop Loss и Take Profit рассчитываются на основе ATR
- Адаптируются к волатильности рынка

---

## 📝 Итоговая схема работы

```
1. Получение данных с биржи
   ↓
2. Расчет индикаторов (EMA, RSI, MACD, ATR)
   ↓
3. Начисление баллов для BUY (long_score)
   ↓
4. Начисление баллов для SELL (short_score)
   ↓
5. Конвертация баллов в вероятности
   ↓
6. Определение типа сигнала (BUY/SELL/HOLD)
   ↓
7. Определение силы сигнала (STRONG/MEDIUM/WEAK)
   ↓
8. Расчет Stop Loss и Take Profit
   ↓
9. Проверка рыночного контекста (опционально)
   ↓
10. Формирование результата
```

---

## ✅ Заключение

Стратегия EMA+RSI+MACD - это комплексная система, которая:
- ✅ Использует несколько индикаторов для подтверждения сигналов
- ✅ Работает по прозрачной балльной системе
- ✅ Адаптируется к волатильности рынка через ATR
- ✅ Защищена от ложных сигналов через фильтры RSI и проверку контекста
- ✅ Генерирует четкие сигналы с определенной силой

Все данные сохраняются в базу данных для последующего анализа и оптимизации стратегии.

