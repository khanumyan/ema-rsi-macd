# Критерии, хранящиеся в базе данных

## Обзор

В таблице `crypto_signals` хранятся **ВСЕ критерии и данные**, которые используются для генерации сигнала по стратегии EMA+RSI+MACD.

## ✅ Сохраняются ВСЕ сигналы

Важно понимать, что в базу данных сохраняются **ВСЕ сигналы**, включая:
- ✅ **BUY** сигналы (сильные, средние и слабые)
- ✅ **SELL** сигналы (сильные, средние и слабые)
- ✅ **HOLD** сигналы (когда нет четкого направления)

Это позволяет анализировать полную историю всех критериев и индикаторов.

## 📊 Структура данных

### 1. Основные данные сигнала

| Поле | Описание | Пример |
|------|----------|--------|
| `symbol` | Символ криптовалюты | BTCUSDT, ETHUSDT |
| `strategy` | Название стратегии | EMA+RSI+MACD |
| `type` | Тип сигнала | BUY, SELL, HOLD |
| `strength` | Сила сигнала | STRONG, MEDIUM, WEAK |
| `price` | Текущая цена закрытия | 50000.00 |

### 2. Индикаторы EMA (Exponential Moving Average)

| Поле | Описание | Формула | Пример |
|------|----------|---------|--------|
| `ema` | EMA(20) - быстрая скользящая средняя | EMA = (Price × 2/(20+1)) + (Previous EMA × (1-2/(20+1))) | 49800.00 |
| `ema_slow` | EMA(50) - медленная скользящая средняя | EMA = (Price × 2/(50+1)) + (Previous EMA × (1-2/(50+1))) | 49500.00 |

**Использование:**
- Если `price > ema` и `ema > ema_slow` → бычий тренд (BUY)
- Если `price < ema` и `ema < ema_slow` → медвежий тренд (SELL)

### 3. Индикатор RSI (Relative Strength Index)

| Поле | Описание | Диапазон | Пример |
|------|----------|----------|--------|
| `rsi` | RSI(14) - индекс относительной силы | 0-100 | 35.50 |

**Использование (НОВАЯ ЛОГИКА через ATR):**
- **BUY**: `rsi BETWEEN 48 AND 60` - оптимальная зона для входа
- **SELL**: `rsi BETWEEN 40 AND 52` - оптимальная зона для выхода
- RSI больше не использует абсолютные пороги перепроданности/перекупленности

**Формула:**
```
RS = Average Gain / Average Loss
RSI = 100 - (100 / (1 + RS))
```

### 4. Индикатор MACD (Moving Average Convergence Divergence)

| Поле | Описание | Формула | Пример |
|------|----------|---------|--------|
| `macd` | MACD Line | EMA(12) - EMA(26) | 120.50 |
| `macd_signal` | Signal Line | EMA(9) от MACD Line | 100.20 |
| `macd_histogram` | Histogram | MACD Line - Signal Line | 20.30 |

**Использование (НОВАЯ ЛОГИКА через ATR):**
- **BUY**: `macd_hist_atr >= 0.25` (где `macd_hist_atr = macd_histogram / atr`)
- **SELL**: `macd_hist_atr >= 0.25` (где `macd_hist_atr = ABS(macd_histogram) / atr`)
- MACD histogram теперь нормализуется через ATR, что делает критерии универсальными для всех коинов

### 5. Индикатор ATR (Average True Range)

| Поле | Описание | Формула | Пример |
|------|----------|---------|--------|
| `atr` | ATR(14) - средний истинный диапазон | Среднее значение True Range за 14 периодов | 500.00 |

**Использование:**
- Для расчета Stop Loss: `price ± (atr × 2.3)`
- Для расчета Take Profit: `price ± (atr × 2.0)`
- **НОРМАЛИЗАЦИЯ**: Все импульсы нормализуются через ATR для универсальности между разными коинами

**True Range = максимум из:**
1. High - Low
2. |High - Previous Close|
3. |Low - Previous Close|

**Нормализованные значения (через ATR):**
- `macd_hist_atr` = `macd_histogram / atr` - MACD histogram в долях ATR
- `ema_distance_atr` = `ABS(price - ema) / atr` - Расстояние цены от EMA в долях ATR
- `atr_pct` = `(atr / price) × 100` - ATR в процентах от цены (фильтр волатильности)

### 6. Балльная система и критерии оценки (НОВАЯ ЛОГИКА через ATR)

| Поле | Описание | Диапазон | Пример |
|------|----------|----------|--------|
| `long_score` | Сумма баллов для BUY | 0-100 | 65 |
| `short_score` | Сумма баллов для SELL | 0-100 | 20 |
| `long_probability` | Вероятность BUY в % | 0-100 | 75 |
| `short_probability` | Вероятность SELL в % | 0-100 | 25 |
| `score_diff` | Разница баллов (long_score - short_score) | -100 до 100 | 15 |

**🎯 НОВАЯ СТРАТЕГИЯ: Все критерии нормализуются через ATR**

#### ✅ BUY условия (ВСЕ должны быть выполнены одновременно):

1. **RSI**: `rsi BETWEEN 48 AND 60` (фиксированный диапазон)

2. **MACD Histogram (нормализованный)**: 
   - `macd_hist_atr >= 0.25`
   - где `macd_hist_atr = macd_histogram / atr`
   - Подтверждает наличие импульса, но не перегрев

3. **EMA Distance (нормализованный)**:
   - `ema_distance_atr BETWEEN 0.5 AND 1.5`
   - где `ema_distance_atr = ABS(price - ema) / atr`
   - `< 0.5` → флет (слишком близко к EMA)
   - `> 1.5` → поздно (слишком далеко от EMA)

4. **Волатильность (ATR в процентах)**:
   - `atr_pct BETWEEN 0.3 AND 3.0`
   - где `atr_pct = (atr / price) × 100`
   - Отсекает мёртвые рынки (< 0.3%) и бешеные рынки (> 3.0%)

5. **Score Difference**:
   - `(long_score - short_score) BETWEEN 10 AND 20`
   - Подтверждает превосходство BUY сигнала, но не экстремальное

#### ✅ SELL условия (ВСЕ должны быть выполнены одновременно):

1. **RSI**: `rsi BETWEEN 40 AND 52` (фиксированный диапазон)

2. **MACD Histogram (нормализованный)**: 
   - `macd_hist_atr >= 0.25`
   - где `macd_hist_atr = ABS(macd_histogram) / atr`
   - Подтверждённый импульс вниз

3. **EMA Distance (нормализованный)**:
   - `ema_distance_atr BETWEEN 0.5 AND 1.5`
   - где `ema_distance_atr = ABS(price - ema) / atr`

4. **Волатильность (ATR в процентах)**:
   - `atr_pct BETWEEN 0.3 AND 3.0`
   - где `atr_pct = (atr / price) × 100`

5. **Score Difference**:
   - `(short_score - long_score) BETWEEN 10 AND 20`
   - Подтверждает превосходство SELL сигнала

**Расчет баллов (для score difference):**
- BUY: +30 за тренд (price > ema && ema > ema_slow), +30 за MACD (macd > 0 && macd_hist > 0), +20 за RSI в диапазоне, +20 за пересечение MACD
- SELL: +30 за тренд (price < ema && ema < ema_slow), +30 за MACD (macd < 0 && macd_hist < 0), +20 за RSI в диапазоне, +20 за пересечение MACD

**Расчет вероятностей (для обратной совместимости):**
```
total_score = long_score + short_score
long_probability = (long_score / total_score) × 100
short_probability = (short_score / total_score) × 100
```

**Определение сигнала (НОВАЯ ЛОГИКА):**
- Если **ВСЕ** BUY условия выполнены → **BUY**
- Если **ВСЕ** SELL условия выполнены → **SELL**
- Иначе → **HOLD**

**Определение силы:**
- Если `|long_probability - short_probability| > 20` → **STRONG**
- Если `|long_probability - short_probability| > 10` → **MEDIUM**
- Иначе → **WEAK**

### 7. Уровни входа/выхода

| Поле | Описание | Формула | Пример |
|------|----------|---------|--------|
| `stop_loss` | Stop Loss уровень | Для BUY: `price - (atr × 2.3)`<br>Для SELL: `price + (atr × 2.3)` | 48850.00 |
| `take_profit` | Take Profit уровень | Для BUY: `price + (atr × 2.0)`<br>Для SELL: `price - (atr × 2.0)` | 51000.00 |

### 8. Параметры запроса данных

| Поле | Описание | По умолчанию | Пример |
|------|----------|--------------|--------|
| `interval` | Таймфрейм свечей | 15m | 15m, 1h, 4h |
| `limit` | Количество свечей | 200 | 200, 500 |

### 9. Метаданные

| Поле | Описание | Пример |
|------|----------|--------|
| `reason` | Текстовое описание причины сигнала | "RSI: 52.50 | MACD Hist/ATR: 0.350 | EMA Dist/ATR: 1.200 | ATR%: 1.50 | Score Diff: 15 | Trend: Bullish | Price above EMA20" |
| `sent_to_telegram` | Отправлен ли сигнал в Telegram | true/false |
| `created_at` | Время создания записи | 2026-02-04 13:15:00 |

## 📈 Пример записи в базе данных

```json
{
  "id": 1,
  "symbol": "BTCUSDT",
  "strategy": "EMA+RSI+MACD",
  "type": "BUY",
  "strength": "STRONG",
  "price": 50000.00,
  
  "rsi": 35.50,
  "ema": 49800.00,
  "ema_slow": 49500.00,
  "macd": 120.50,
  "macd_signal": 100.20,
  "macd_histogram": 20.30,
  "atr": 500.00,
  "macd_hist_atr": 0.0406,
  "ema_distance_atr": 1.200,
  "atr_pct": 1.00,
  "score_diff": 15,
  
  "stop_loss": 48850.00,
  "take_profit": 51000.00,
  
  "long_score": 65,
  "short_score": 0,
  "long_probability": 100,
  "short_probability": 0,
  
  "interval": "15m",
  "limit": 200,
  
  "reason": "RSI: 52.50 | MACD Hist/ATR: 0.350 | EMA Dist/ATR: 1.200 | ATR%: 1.50 | Score Diff: 15 | Trend: Bullish | Price above EMA20",
  "sent_to_telegram": true,
  "created_at": "2026-02-04 13:15:00"
}
```

## 🔍 Использование данных для анализа

Все сохраненные критерии можно использовать для:

1. **Анализа эффективности стратегии:**
   - Какие комбинации индикаторов дают лучшие результаты?
   - При каких значениях RSI/MACD сигналы наиболее точны?

2. **Оптимизации параметров:**
   - Какие значения `long_score`/`short_score` коррелируют с успешными сделками?
   - Какой `strength` (STRONG/MEDIUM/WEAK) дает лучшие результаты?

3. **Обратного тестирования:**
   - Можно пересчитать сигналы с другими параметрами
   - Можно изменить логику балльной системы

4. **Статистики:**
   - Сколько сигналов каждого типа было сгенерировано?
   - Какая средняя вероятность у успешных сигналов?

## 🎯 Преимущества новой логики через ATR

### ❌ Проблемы старой логики:
- **Абсолютные значения**: BTC ≠ DOGE, сигналы терялись или мусорились
- **Фиксированные пороги**: Одни и те же для всех коинов
- **Игнорирование волатильности**: Не учитывалась текущая волатильность рынка
- **Много NULL сигналов**: Из-за несоответствия масштабов разных коинов

### ✅ Преимущества новой логики:
- **Универсальность**: Одинаковая логика для всех коинов (BTC, DOGE, ETH и т.д.)
- **Адаптивность**: Критерии автоматически подстраиваются под волатильность рынка
- **Меньше мусора**: Резкое падение количества NULL сигналов
- **Рыночные сигналы**: Сигналы становятся рыночными, а не математическими
- **Фильтрация**: Отсекаются мёртвые (< 0.3% ATR) и бешеные (> 3.0% ATR) рынки

### 📊 Что изменилось:

| Параметр | Было | Стало |
|----------|------|-------|
| RSI для BUY | Любой / фиксированный | 48-60 (строгий диапазон) |
| RSI для SELL | < 30 | 40-52 (строгий диапазон) |
| MACD Histogram | Абсолютное число | % от ATR (>= 0.25) |
| EMA Distance | Проценты от цены | % от ATR (0.5-1.5) |
| Волатильность | Не учитывалась | ATR% (0.3-3.0) |
| Score Difference | Не использовался | 10-20 (обязательное условие) |

## ✅ Итог

В базе данных хранятся **ВСЕ критерии**, необходимые для:
- ✅ Понимания, почему был сгенерирован сигнал
- ✅ Воспроизведения расчета сигнала
- ✅ Анализа и оптимизации стратегии
- ✅ Обратного тестирования с другими параметрами
- ✅ **НОВОЕ**: Универсальной работы со всеми коинами через ATR нормализацию






